pipeline {
    agent any

    stages{
        stage('build') {
            steps {
                script{ 
                    sh "ls -lrt"
                    sh "mvn install"
                    sh "ls -lrt"
                }
            }
        }
    }
}

